#include "main.h"
/**
 * add - plus
 * @a: input from main
 * @b: same
 *
 * adds two numbers
 * Return: always 0
 */
int add(int a, int b)
{
	int sum;
	int tens;
	int ones;

	sum = a + b;
	return (sum);
}
