#include "main.h"

char *_strcat(char *dest, char *src)
{
int i;

int j = _strlen(dest);

for (i = 0; src[i] != '\0'; i++)
{
dest[i + j] = src[i];
}
dest[i + j] = '\0';
return (dest);
}
int _strlen(char *s)
{
int i;

for (i = 0; s[i] != '\0'; ++i)
{

}
return (i);
}
